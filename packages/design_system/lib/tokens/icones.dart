abstract final class DsIcones {
  /// Ícones inline de contexto — datas, labels, chips (14px)
  static const double xs = 14;

  /// Ícones de controle — clear/close em inputs (16px)
  static const double sm = 16;

  /// Ícones em botões (18px)
  static const double md = 18;

  /// Ícones de ação em cards, snackbar (20px)
  static const double lg = 20;

  /// Ícones de estado vazio — ilustração (64px)
  static const double xl = 64;
}
