abstract final class DsAnimacoes {
  /// Duração padrão de snackbar de sucesso e info — 3 segundos
  static const Duration snackbarCurta = Duration(seconds: 3);

  /// Duração padrão de snackbar de erro — 4 segundos
  static const Duration snackbarLonga = Duration(seconds: 4);
}
