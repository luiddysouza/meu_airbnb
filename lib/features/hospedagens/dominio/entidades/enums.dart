/// Status de uma hospedagem no ciclo de vida.
enum StatusHospedagem { confirmada, pendente, cancelada, concluida }

/// Plataforma de origem da reserva.
enum Plataforma { airbnb, booking, direto, outro }
