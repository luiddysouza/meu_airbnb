import 'package:flutter/material.dart';

/// Página principal de hospedagens.
///
/// Placeholder para o Commit 13, onde será conectada ao SduiCubit
/// e à árvore de widgets SDUI.
class HospedagensPagina extends StatelessWidget {
  const HospedagensPagina({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('HospedagensPagina — em construção')),
    );
  }
}
